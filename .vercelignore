# Do not upload build artifacts — they are produced by buildCommand on Vercel.
# Use a leading slash so only the repo-root folders match (not client/public).
/public
/dist
.vercel
.env*.local
.env.vercel.secrets
node_modules
